# Debug Journal — AArch64 page fault loop
Started: 2026-09-03
Goal: Identify and fix the RPi3 page fault loop involving 0x3f215054.

## Environment snapshot
- Runtime: Rust AArch64 kernel, Docker build, RPi3 serial console
- Entry: build MCP, deploy to /mnt/d/pi_sd, power-cycle RPi3
- Git HEAD: fa4158a5
- References read: Rust runtime, debugging setup, debugging investigation

## Hypotheses
1. [ACTIVE] EL1 diagnostic output faults on low physical UART VA 0x3f215054 — confirm by replacing it with the mapped PL011 VA — fix diagnostic writer.
2. [OPEN] The original 0x2bfc000 is a failing kernel access to user memory — confirm from post-fix ELR/FAR output — fix originating access.
3. [REFUTED] Lower-EL exception return retries through sync_exception_current — refuted by trap.S lower-EL return path — no fix.

## Findings
- `trap.S` lower-EL exceptions save `RawUserContext` and return to `run_user`; `sync_exception_current` is not called for them.
- `sync_exception_current` diagnostic `put_char!` accesses low VAs 0x3f215054 and 0x3f215040.
- Runtime RPi3 serial uses PL011 through `KERNEL_BASE_VADDR`.

## Artifacts to revert
- [ ] `ostd/src/arch/aarch64/trap/mod.rs` — use mapped PL011 diagnostic addresses; revert the diagnostic writer hunk if needed.
- [ ] `kernel/src/thread/exception.rs` — temporary user exception warning; remove after capturing ESR/IP/FAR.
- [ ] `ostd/src/arch/aarch64/trap/mod.rs` — temporary EL1 fault warning; remove after capturing ESR/ELR/FAR.
- [ ] `/tmp/asterinas-init-extract` — temporary initramfs extraction for ELF inspection; remove after inspection.
- [ ] `.debug-journal.md` — remove after debugging.

## Final fix
