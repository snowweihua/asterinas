[permissions]
allow = ["Edit", "Bash(which bwrap || dpkg -l bubblewrap 2>/dev/null || echo \"bwrap not found\")", "Bash(sudo apt-get install -y bubblewrap 2>&1 | tail -3)", "Bash(mkdir -p /home/snow/asterinas/.reasonix && cat > /home/snow/asterinas/.reasonix/config.toml << 'EOF'\n[sandbox]\nbash = \"off\"\nEOF\necho \"done\")", "Bash(dpkg -l bwrap 2>/dev/null || which bwrap 2>/dev/null || apt list --installed 2>/dev/null | grep bubblewrap || echo \"NOT_FOUND\")", "Bash(echo \"bash works\" && git --version && which docker)", "Bash(cd /home/snow/asterinas && git add -A && git commit -m \"aarch64/diag: add stack canary and pl011_puts_hex to alloc_frame_with — trap FP/LR at entry/exit to pinpoint stack corruption source\")", "Bash(cd /home/snow/asterinas && docker run --rm -v $(pwd):/root/asterinas asterinas/aarch64-dev:latest bash -c 'cd /root/asterinas && cargo osdk build --release --target-arch aarch64 --boot-method qemu-direct --scheme aarch64' 2>&1)", "Bash(cd /home/snow/asterinas && git add -A && git commit -m \"aarch64/diag: fix scope error in stack canary — use single static pair for both entry and exit\")", "Bash(cd /home/snow/asterinas && git add -A && git commit -m \"aarch64/diag: fix strb register class — use in(\\\"w3\\\") instead of in(reg) to ensure 32-bit register\")"]
